import 'package:flutter/material.dart';

class UangElektronik extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Uang Elektonik"),
      ),
      body: Center(child: Text('Uang Elektonik Page')),
    );
  }
}
